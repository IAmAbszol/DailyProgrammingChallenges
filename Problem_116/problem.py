'''
    The main wall of this problem is the O(1) time constraint which can be bypassed
    by allowing Python to generate on startup.

    To do this, we could use the property declaration in Python which assembles this on startup.
'''